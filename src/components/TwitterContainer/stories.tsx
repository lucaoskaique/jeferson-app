import { Story, Meta } from '@storybook/react'

import TwitterContainer from '.'

export default {
  title: 'TwitterContainer',
  component: TwitterContainer
} as Meta

export const Default: Story = () => <TwitterContainer />
