import { Story, Meta } from '@storybook/react'

import SubscribeCard from '.'

export default {
  title: 'SubscribeCard',
  component: SubscribeCard
} as Meta

export const Default: Story = () => <SubscribeCard />
